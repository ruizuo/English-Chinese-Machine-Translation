# Introduction
In this project, I would like to use recurrent neural networks to build a language translation system. The original language is English, while the target language is Chinese.

# Setup

This project requires a GPU to accelerate the training process.

## Environment
- Python 3
- NumPy
- TensorFlow 1.10.0
- Keras 2.0.8
